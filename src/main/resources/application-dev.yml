#spring boot 开发环境下 配置文件
server:
  port: 80
  context-path: /book-store
spring:
  datasource:
    dirver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book_store?characterEncoding=utf8&useSSL=true
    username: root
    password: ironman2014
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
